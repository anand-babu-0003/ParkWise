'use client';

import { MobileLayout } from '@/app/mobile-layout';
import MobileSetup from '@/app/mobile-setup';

export default function SetupPage() {
  return (
    <MobileLayout>
      <MobileSetup />
    </MobileLayout>
  );
}
